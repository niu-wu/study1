<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study11.dao.EmployeeContractAttachmentDao">

    <resultMap id="EmployeeContractAttachmentMap" type="com.example.study11.entity.po.EmployeeContractAttachmentPo">
        <id property="attachmentUuid" column="attachment_uuid"/>
        <result property="id" column="id"/>
        <result property="employeeUuid" column="employee_uuid"/>
        <result property="attachmentType" column="attachment_type"/>
        <result property="fileName" column="file_name"/>
        <result property="fileUrl" column="file_url"/>
        <result property="fileType" column="file_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.study11.entity.po.EmployeeContractAttachmentPo"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO `employee_contract_attachment`
        (attachment_uuid, employee_uuid, attachment_type, file_name, file_url,
         file_type, file_size, sort_order, created_by, created_at, updated_by, updated_at)
        VALUES
        (#{attachmentUuid}, #{employeeUuid}, #{attachmentType}, #{fileName}, #{fileUrl},
         #{fileType}, #{fileSize}, #{sortOrder}, #{createdBy}, NOW(), #{createdBy}, NOW())
    </insert>

    <select id="selectByUuid" resultMap="EmployeeContractAttachmentMap">
        SELECT * FROM `employee_contract_attachment`
        WHERE attachment_uuid = #{attachmentUuid} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByEmployeeUuid" resultMap="EmployeeContractAttachmentMap">
        SELECT * FROM `employee_contract_attachment`
        WHERE employee_uuid = #{employeeUuid} AND deleted = 0
        ORDER BY sort_order ASC, id ASC
    </select>

    <update id="updateByUuid" parameterType="com.example.study11.entity.po.EmployeeContractAttachmentPo">
        UPDATE `employee_contract_attachment`
        SET attachment_type = #{attachmentType},
            file_name       = #{fileName},
            file_url        = #{fileUrl},
            file_type       = #{fileType},
            file_size       = #{fileSize},
            sort_order      = #{sortOrder},
            updated_by      = #{updatedBy},
            updated_at      = NOW()
        WHERE attachment_uuid = #{attachmentUuid} AND deleted = 0
    </update>

    <update id="deleteByUuid">
        UPDATE `employee_contract_attachment`
        SET deleted = 1, updated_by = #{updatedBy}, updated_at = NOW()
        WHERE attachment_uuid = #{attachmentUuid} AND deleted = 0
    </update>
</mapper>
