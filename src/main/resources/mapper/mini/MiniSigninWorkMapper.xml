<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.study11.mini.dao.MiniSigninWorkDao">

    <insert id="batchInsert">
        INSERT INTO `mini_signin_work`
        (signin_uuid, start_date, end_date, company_name, position, leave_reason, sort_order)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.signinUuid}, #{item.startDate}, #{item.endDate},
             #{item.companyName}, #{item.position}, #{item.leaveReason}, #{item.sortOrder})
        </foreach>
    </insert>

    <select id="selectBySigninUuid" resultType="com.example.study11.mini.entity.po.MiniSigninWorkPo">
        SELECT * FROM `mini_signin_work`
        WHERE signin_uuid = #{signinUuid} AND deleted = 0
        ORDER BY sort_order ASC
    </select>

    <delete id="deleteBySigninUuid">
        UPDATE `mini_signin_work`
        SET deleted = 1
        WHERE signin_uuid = #{signinUuid} AND deleted = 0
    </delete>
</mapper>
